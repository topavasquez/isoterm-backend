const config = {
    development: {
        username:'postgres',
        password:'1234',
        database:'isoterm',
        host:'localhost',
        dialect:'postgres'
    }
}

module.exports = config